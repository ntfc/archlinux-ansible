- hosts: localhost
  connection: local
  roles:
    - role: pacman
      tags: ['pacman', 'aur']
    - role: sudo
      tags: ['sudo']
    - role: locale
      tags: ['locale', 'timezone', 'ntp']
    - role: network
      tags: ['network']

  # power / suspend / laptop power
  # nvidia
