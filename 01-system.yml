- hosts: localhost
  connection: local
  roles:
    - role: pacman
      tags: ['pacman', 'aur']
    - role: sudo
      tags: ['sudo']
    - role: locale
      tags: ['locale', 'timezone', 'ntp']

  # power / suspend / laptop power
  # nvidia
